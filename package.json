{
  "name": "retail-analytic-app",
  "version": "1.0.0",
  "description": "Root workspace workspace wrapper for deploying fullstack React + Express app in a single web service",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:backend && npm run install:frontend && npm run build:frontend",
    "start": "node backend/server.js"
  }
}
